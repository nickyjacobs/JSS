# Intrusion Detection Monitor (IDM)
# Copy to .env and adjust if needed

IDM_HOST=127.0.0.1
IDM_PORT=8006
IDM_AUTH_LOG=/var/log/auth.log
IDM_FAILED_LOGIN_THRESHOLD=5
IDM_TIME_WINDOW=300
IDM_ALERT_COOLDOWN=60
